const A = ['A', 'B', 'C', 'D', 'E', 'F']
const B = [1,   5,   4,   3,   2,   0]

function sort(items, newOrder) {

  for (let i = 0; i < items.length; i++){
    let to = newOrder[i]

      ;[[items[to],[items[items[i]]],[]]=[]
  }

}
